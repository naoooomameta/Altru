# Microsoft Clarity Data Export API token.
# Generate from: Clarity dashboard > Settings > Data Export > Generate new API token
CLARITY_API_TOKEN=

# Number of days to fetch (1-3). Defaults to 1.
CLARITY_NUM_DAYS=1
